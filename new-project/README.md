# new-project

A simple standalone Maven Java project scaffolded inside the repository.

## Build and test

```bash
mvn -f new-project/pom.xml test
```

## Run

```bash
mvn -f new-project/pom.xml package
java -cp new-project/target/classes com.example.newproject.App
```
